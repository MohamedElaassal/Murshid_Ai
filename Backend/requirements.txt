# MOURCHID-AI — Hackathon MVP
# Install with:  pip install -r requirements.txt

# Web Framework & API
fastapi>=0.110.0
uvicorn>=0.29.0
httpx>=0.27.0
pydantic>=2.6.0
python-dotenv>=1.0.1

# Database
SQLAlchemy>=2.0.29

# AI & Vision
torch>=2.0.0
torchvision>=0.15.0
pillow>=10.0.0
litellm>=1.40.0
elevenlabs>=1.0.0

# RAG & Document Processing
pandas>=2.0.0
lxml>=4.9.0
beautifulsoup4>=4.12.0

# (Optional) For RAG later
# langchain>=0.1.0
# faiss-cpu>=1.8.0
# pypdf>=4.1.0

# ─── QUICK START ───────────────────────────────────────────────────────────────
#
# 1. Create a .env file (or export vars):
#
#    export TELEGRAM_BOT_TOKEN="7xxxxxxxxx:AAF..."
#    export ELEVENLABS_API_KEY="..."          # optional TTS voice replies
#    export ELEVENLABS_VOICE_ID="..."         # optional; default in speech.py
#
# 2. Run the server:
#    uvicorn main:app --reload
#
# 3. Expose your local server using ngrok:
#    ngrok http 8000
#
# 4. Set the Telegram webhook to your ngrok URL:
#    python set_webhook.py "https://<your-ngrok-url>.ngrok-free.app/webhook"
#
# 5. On-stage demo flow:
#    a. Send any crop photo to the bot → get instant Darija diagnosis
#    b. Reply "1" → CLOSED_SUCCESS   /   Reply "2" → CLOSED_FAILED
